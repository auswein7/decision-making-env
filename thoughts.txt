THIS FILE CONTAINS ALL FUTURE IDEAS FOR CODE ADDITIONS

ADDITIONS BEFORE NEXT MEETING

    1. Ground truth
        - Need an algorithm to compare future experiments against
        - Add new functions to app/core/algorithms.py
        - Algos to add:
            - approximate best response
                - add param to app.props: beta
                - multiply the best action of the agent in the last N iterations against beta
                - 0 <= beta <= 1
                - Gives a range from action_score*beta -> action_score
                - randomly select any action from within this range
            - (M)ILP
                - read vaccine n-times paper agin to implement this
            - Brute force
                - test all possible agent allocations and find the best score
        - Compare any future algorithm scores to above algorithms

    2. Data Visualization and Collection
        - Need to show the relation of system score to all values passed by application.properties
        - Need to export data from simulation

    3. Save System state from random gen.
        - Add option to save the system and final coverage of the experiment to json file

    4. Add runner module
        - Make running the simulations easier, add more parameters etc.

    5. Make sure the python max() is not deterministically breaking ties when choosing agent action

    6. Add algorithm to be used to evaluate system score/agent action to the passed scenario.json
        - need to parse additional data from json in handler

    APP.PROPS PARAMS

    7. Beta: for approximate best response algo
    8. Threshold score: save the system configuration if the score is above the threshold
    9. Generate visualizations: boolean, enable graphical output or not


FUTURE EXPERIMENT THOUGHTS

    1. Adding spacial/temporal feature
        - 2D grid representation of resource locations
            - penalize agents if travel distance is large
            - add animations
        - Not sure how to add time
            - penalize agents if travel time is large

    2. Adding parallel vs sequential agent wake up
        - What if all agents act at the same time? what if a subset of them do?
        - What if only one agent can act at a time?

ESSENTIAL QUESTIONS

    1. What kind of experiments are we going to run?
    2. What heuristic can we use to evaluate these algorithms?
    3. How many runs does it take for system score to converge given a static system configuration?
    4. What are the upper bounds on resource/agent count in each system?
    5. What parameters will make this algorithm perform best given this system configuration?
    6. How does the size of the agents action sets affect overall system score? (1 -> all)

THOUGHTS FROM WORKSHOP

    1. Define two functions
        - w(|a|_r): "the maximum cover", input is how many agents are covering a resource
            if w(2) = 1, the value of the resource is its initial value if two agents are covering it
            if w(2) = 2, the value of the resource is doubled if two agents are covering it
            if w(1) = 0, the value of the resource is zero if only one agent is covering it
        - f(|a|_r): applies to system objective function
            Look more into what this function is doing



